{
  "_comment": "Template for proxy-creds-gen. ${VAR} placeholders are resolved from env vars.",
  "example-mlflow.yourcompany.com": {
    "Authorization": "Bearer ${MLFLOW_TRACKING_TOKEN}"
  },
  "example-ray.yourcompany.com": {
    "Authorization": "Bearer ${RAY_SERVICE_TOKEN}"
  }
}
