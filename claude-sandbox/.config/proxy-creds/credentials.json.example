{
  "_comment": "Map: hostname -> { header-name: header-value }. Real tokens injected by proxy; sandbox never sees them.",
  "example-mlflow.yourcompany.com": {
    "Authorization": "Bearer REPLACE_WITH_MLFLOW_TRACKING_TOKEN"
  },
  "example-ray.yourcompany.com": {
    "Authorization": "Bearer REPLACE_WITH_RAY_SERVICE_TOKEN"
  }
}
